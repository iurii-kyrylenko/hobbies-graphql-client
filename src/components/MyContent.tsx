import { Box, Typography } from "@mui/material";

const MyContent = () => {
    return (
        <Box component="main" sx={{ p: 3 }}>
            <Typography>
                CONTENT
            </Typography>
        </Box>
    );
};

export default MyContent;
